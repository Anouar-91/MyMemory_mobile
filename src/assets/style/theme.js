import { extendTheme } from "native-base";

const theme = extendTheme({
  colors: {
    brandPrimary: "#FF0000", // Remplacez cette valeur par votre couleur primaire personnalisée
  },
});

export default theme;